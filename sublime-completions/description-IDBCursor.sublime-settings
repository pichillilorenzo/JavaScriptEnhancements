{
	"scope": "source.js",
	"completions": [
		[
			"description-advance()\tIDBCursor",
			{
				"name": "advance",
				"return_type": "void",
				"parameters": [
					{
						"name": "count",
						"type": "unsigned long",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "If the value for count is 0 (zero), the implementation MUST throw a TypeError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError. If the cursor's source or effective object store has been deleted, the implementation MUST throw a DOMException of type InvalidStateError.",
				"is_static": false,
				"type": "operation",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#advance",
				"full_description": "/*\n\n\tIDBCursor\n\n\tadvance ( count: unsigned long )\n\n\tReturn Type:\n\tvoid\n\n\tDescription:\n\tIf the value for count is 0 (zero), the implementation MUST throw a TypeError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError. If the cursor's source or effective object store has been deleted, the implementation MUST throw a DOMException of type InvalidStateError.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#advance\n\n*/"
			}
		],
		[
			"description-primaryKey\tIDBCursor",
			{
				"name": "primaryKey",
				"return_type": "any",
				"description": "Returns the cursor's current effective key. Note that if this property returns an object (specifically an Array), it returns the same object instance every time it is inspected, until the cursor's effective key is changed. This means that if the object is modified, those modifications will be seen by anyone inspecting the value of the cursor.",
				"is_static": false,
				"type": "property",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#primaryKey",
				"full_description": "/*\n\n\tIDBCursor\n\n\tprimaryKey\n\n\tReturn Type:\n\tany\n\n\tDescription:\n\tReturns the cursor's current effective key. Note that if this property returns an object (specifically an Array), it returns the same object instance every time it is inspected, until the cursor's effective key is changed. This means that if the object is modified, those modifications will be seen by anyone inspecting the value of the cursor.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#primaryKey\n\n*/"
			}
		],
		[
			"description-key\tIDBCursor",
			{
				"name": "key",
				"return_type": "any",
				"description": "Returns the cursor's current key. Note that if this property returns an object (specifically an Array), it returns the same object instance every time it is inspected, until the cursor's key is changed. This means that if the object is modified, those modifications will be seen by anyone inspecting the value of the cursor.",
				"is_static": false,
				"type": "property",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#key",
				"full_description": "/*\n\n\tIDBCursor\n\n\tkey\n\n\tReturn Type:\n\tany\n\n\tDescription:\n\tReturns the cursor's current key. Note that if this property returns an object (specifically an Array), it returns the same object instance every time it is inspected, until the cursor's key is changed. This means that if the object is modified, those modifications will be seen by anyone inspecting the value of the cursor.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#key\n\n*/"
			}
		],
		[
			"description-update()\tIDBCursor",
			{
				"name": "update",
				"return_type": "IDBRequest",
				"parameters": [
					{
						"name": "value",
						"type": "any",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "If the transaction this IDBCursor belongs to has its mode set to \"readonly\", the implementation MUST throw a DOMException of type ReadOnlyError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError.",
				"is_static": false,
				"type": "operation",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#update",
				"full_description": "/*\n\n\tIDBCursor\n\n\tupdate ( value: any )\n\n\tReturn Type:\n\tIDBRequest\n\n\tDescription:\n\tIf the transaction this IDBCursor belongs to has its mode set to \"readonly\", the implementation MUST throw a DOMException of type ReadOnlyError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#update\n\n*/"
			}
		],
		[
			"description-continue()\tIDBCursor",
			{
				"name": "continue",
				"return_type": "void",
				"parameters": [
					{
						"name": "key",
						"type": "any",
						"is_optional": true,
						"description": ""
					}
				],
				"description": "If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError. If the cursor's source or effective object store has been deleted, the implementation MUST throw a DOMException of type InvalidStateError.",
				"is_static": false,
				"type": "operation",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#continue",
				"full_description": "/*\n\n\tIDBCursor\n\n\tcontinue ( key?: any )\n\n\tReturn Type:\n\tvoid\n\n\tDescription:\n\tIf the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError. If the cursor's source or effective object store has been deleted, the implementation MUST throw a DOMException of type InvalidStateError.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#continue\n\n*/"
			}
		],
		[
			"description-delete()\tIDBCursor",
			{
				"name": "delete",
				"return_type": "IDBRequest",
				"parameters": [],
				"description": "If the transaction this IDBCursor belongs to has its mode set to \"readonly\", the implementation MUST throw a DOMException of type ReadOnlyError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError.",
				"is_static": false,
				"type": "operation",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#delete",
				"full_description": "/*\n\n\tIDBCursor\n\n\tdelete ()\n\n\tReturn Type:\n\tIDBRequest\n\n\tDescription:\n\tIf the transaction this IDBCursor belongs to has its mode set to \"readonly\", the implementation MUST throw a DOMException of type ReadOnlyError. If the transaction this IDBCursor belongs to is not active, the implementation MUST throw a DOMException of type TransactionInactiveError.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#delete\n\n*/"
			}
		],
		[
			"description-source\tIDBCursor",
			{
				"name": "source",
				"return_type": "(IDBObjectStore or IDBIndex)",
				"description": "On getting, returns the IDBObjectStore or IDBIndex which this cursor is iterating. This function never returns null or throws an exception, even if the cursor is currently being iterated, has iterated past its end, or its transaction is not active.",
				"is_static": false,
				"type": "property",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#source",
				"full_description": "/*\n\n\tIDBCursor\n\n\tsource\n\n\tReturn Type:\n\t(IDBObjectStore or IDBIndex)\n\n\tDescription:\n\tOn getting, returns the IDBObjectStore or IDBIndex which this cursor is iterating. This function never returns null or throws an exception, even if the cursor is currently being iterated, has iterated past its end, or its transaction is not active.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#source\n\n*/"
			}
		],
		[
			"description-direction\tIDBCursor",
			{
				"name": "direction",
				"return_type": "IDBCursorDirection",
				"description": "On getting, provide the traversal direction of the cursor.",
				"is_static": false,
				"type": "property",
				"url_doc": "http://html5index.org/Indexed%20DB%20-%20IDBCursor.html#direction",
				"full_description": "/*\n\n\tIDBCursor\n\n\tdirection\n\n\tReturn Type:\n\tIDBCursorDirection\n\n\tDescription:\n\tOn getting, provide the traversal direction of the cursor.\n\n\tURL doc:\n\thttp://html5index.org/Indexed%20DB%20-%20IDBCursor.html#direction\n\n*/"
			}
		]
	]
}