{
	"scope": "source.js",

	"completions":
	[
    ["description-EventTarget.addEventListener()\tEventTarget", "/*\n\tDescription:\n\tThe EventTarget.addEventListener() method registers the specified listener on the EventTarget it's called on. The event target may be an Element in a document, the Document itself, a window, or any other object that supports events (such as XMLHttpRequest).\n\n\tSyntax:\n\ttarget.addEventListener(type, listener[, useCapture]);\n\ttarget.addEventListener(type, listener[, useCapture, wantsUntrusted  ]); // Gecko/Mozilla only\n*/"],
    ["description-EventTarget.dispatchEvent()\tEventTarget", "/*\n\tDescription:\n\tDispatches an Event at the specified EventTarget, invoking the affected EventListeners in the appropriate order. The normal event processing rules (including the capturing and optional bubbling phase) apply to events dispatched manually with dispatchEvent().\n\n\tSyntax:\n\tcancelled = !target.dispatchEvent(event)\n*/"],
    ["description-EventTarget.removeEventListener()\tEventTarget", "/*\n\tDescription:\n\tRemoves the event listener previously registered with EventTarget.addEventListener.\n\n\tSyntax:\n\ttarget.removeEventListener(type, listener[, useCapture])\n*/"],
    ["description-addEventListener()\tEventTarget", "/*\n\tDescription:\n\tThe EventTarget.addEventListener() method registers the specified listener on the EventTarget it's called on. The event target may be an Element in a document, the Document itself, a window, or any other object that supports events (such as XMLHttpRequest).\n\n\tSyntax:\n\ttarget.addEventListener(type, listener[, useCapture]);\n\ttarget.addEventListener(type, listener[, useCapture, wantsUntrusted  ]); // Gecko/Mozilla only\n*/"],
    ["description-dispatchEvent()\tEventTarget", "/*\n\tDescription:\n\tDispatches an Event at the specified EventTarget, invoking the affected EventListeners in the appropriate order. The normal event processing rules (including the capturing and optional bubbling phase) apply to events dispatched manually with dispatchEvent().\n\n\tSyntax:\n\tcancelled = !target.dispatchEvent(event)\n*/"],
    ["description-removeEventListener()\tEventTarget", "/*\n\tDescription:\n\tRemoves the event listener previously registered with EventTarget.addEventListener.\n\n\tSyntax:\n\ttarget.removeEventListener(type, listener[, useCapture])\n*/"]
	]
}