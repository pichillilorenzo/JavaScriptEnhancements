{
	"scope": "source.js",
	"completions": [
		[
			"description-insertItemBefore()\tSVGPointList",
			{
				"name": "insertItemBefore",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "newItem",
						"type": "SVGPoint",
						"is_optional": false,
						"description": ""
					},
					{
						"name": "index",
						"type": "unsigned long",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Inserts a new item into the list at the specified position. The first item is number 0. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. If the item is already in this list, note that the index of the item to insert before is before the removal of the item.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#insertItemBefore",
				"full_description": "/*\n\n\tSVGPointList\n\n\tinsertItemBefore ( newItem: SVGPoint, index: unsigned long )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tInserts a new item into the list at the specified position. The first item is number 0. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. If the item is already in this list, note that the index of the item to insert before is before the removal of the item.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#insertItemBefore\n\n*/"
			}
		],
		[
			"description-replaceItem()\tSVGPointList",
			{
				"name": "replaceItem",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "newItem",
						"type": "SVGPoint",
						"is_optional": false,
						"description": ""
					},
					{
						"name": "index",
						"type": "unsigned long",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Replaces an existing item in the list with a new item. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. If the item is already in this list, note that the index of the item to replace is before the removal of the item.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#replaceItem",
				"full_description": "/*\n\n\tSVGPointList\n\n\treplaceItem ( newItem: SVGPoint, index: unsigned long )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tReplaces an existing item in the list with a new item. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. If the item is already in this list, note that the index of the item to replace is before the removal of the item.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#replaceItem\n\n*/"
			}
		],
		[
			"description-initialize()\tSVGPointList",
			{
				"name": "initialize",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "newItem",
						"type": "SVGPoint",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Clears all existing current items from the list and re-initializes the list to hold the single item specified by the parameter. If the inserted item is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#initialize",
				"full_description": "/*\n\n\tSVGPointList\n\n\tinitialize ( newItem: SVGPoint )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tClears all existing current items from the list and re-initializes the list to hold the single item specified by the parameter. If the inserted item is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#initialize\n\n*/"
			}
		],
		[
			"description-removeItem()\tSVGPointList",
			{
				"name": "removeItem",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "index",
						"type": "unsigned long",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Removes an existing item from the list. Parametersunsigned long index The index of the item which is to be removed. The first item is number 0. Returns The removed item. ExceptionsDOMException, code NO_MODIFICATION_ALLOWED_ERR Raised when the list cannot be modified.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#removeItem",
				"full_description": "/*\n\n\tSVGPointList\n\n\tremoveItem ( index: unsigned long )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tRemoves an existing item from the list. Parametersunsigned long index The index of the item which is to be removed. The first item is number 0. Returns The removed item. ExceptionsDOMException, code NO_MODIFICATION_ALLOWED_ERR Raised when the list cannot be modified.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#removeItem\n\n*/"
			}
		],
		[
			"description-appendItem()\tSVGPointList",
			{
				"name": "appendItem",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "newItem",
						"type": "SVGPoint",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Inserts a new item at the end of the list. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. ParametersSVGPoint newItem The item which is to be inserted.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#appendItem",
				"full_description": "/*\n\n\tSVGPointList\n\n\tappendItem ( newItem: SVGPoint )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tInserts a new item at the end of the list. If newItem is already in a list, it is removed from its previous list before it is inserted into this list. The inserted item is the item itself and not a copy. ParametersSVGPoint newItem The item which is to be inserted.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#appendItem\n\n*/"
			}
		],
		[
			"description-getItem()\tSVGPointList",
			{
				"name": "getItem",
				"return_type": "SVGPoint",
				"parameters": [
					{
						"name": "index",
						"type": "unsigned long",
						"is_optional": false,
						"description": ""
					}
				],
				"description": "Returns the specified item from the list. The returned item is the item itself and not a copy. Any changes made to the item are immediately reflected in the list. Parametersunsigned long index The index of the item from the list which is to be returned.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#getItem",
				"full_description": "/*\n\n\tSVGPointList\n\n\tgetItem ( index: unsigned long )\n\n\tReturn Type:\n\tSVGPoint\n\n\tDescription:\n\tReturns the specified item from the list. The returned item is the item itself and not a copy. Any changes made to the item are immediately reflected in the list. Parametersunsigned long index The index of the item from the list which is to be returned.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#getItem\n\n*/"
			}
		],
		[
			"description-clear()\tSVGPointList",
			{
				"name": "clear",
				"return_type": "void",
				"parameters": [],
				"description": "Clears all existing current items from the list, with the result being an empty list. ExceptionsDOMException, code NO_MODIFICATION_ALLOWED_ERR Raised when the list cannot be modified.",
				"type": "operation",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#clear",
				"full_description": "/*\n\n\tSVGPointList\n\n\tclear ()\n\n\tReturn Type:\n\tvoid\n\n\tDescription:\n\tClears all existing current items from the list, with the result being an empty list. ExceptionsDOMException, code NO_MODIFICATION_ALLOWED_ERR Raised when the list cannot be modified.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#clear\n\n*/"
			}
		],
		[
			"description-numberOfItems\tSVGPointList",
			{
				"name": "numberOfItems",
				"return_type": "unsigned long",
				"description": "The number of items in the list.",
				"type": "property",
				"url_doc": "http://html5index.org/SVG%20-%20SVGPointList.html#numberOfItems",
				"full_description": "/*\n\n\tSVGPointList\n\n\tnumberOfItems\n\n\tReturn Type:\n\tunsigned long\n\n\tDescription:\n\tThe number of items in the list.\n\n\tURL doc:\n\thttp://html5index.org/SVG%20-%20SVGPointList.html#numberOfItems\n\n*/"
			}
		]
	]
}